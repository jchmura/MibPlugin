package pl.jakubchmura.snmp.mib.psi;

import com.intellij.psi.tree.TokenSet;

import static pl.jakubchmura.snmp.mib.psi.SmiTypes.*;

public class SmiTokenSet {

    public static final TokenSet KEYWORDS = TokenSet.create(ABSENT, ACCESS, AGENT_CAPABILITIES, APPLICATION, AUGMENTS,
            BEGIN, BIT, BITS, BY, CHOICE, COMPONENTS, CONTACT_INFO, CREATION_REQUIRES, DEFAULT, DEFINED, DEFINITIONS,
            DEFVAL, DESCRIPTION, DISPLAY_HINT, END, ENUMERATED, ENTERPRISE, EXPLICIT, EXPORTS, FALSE, FROM, GROUP,
            IMPLICIT, IMPLIED, IMPORTS, INCLUDES, INDEX, LAST_UPDATED, MANDATORY_GROUPS, MAX, MAX_ACCESS, MIN, MIN_ACCESS,
            MINUS_INFINITY, MODULE, MODULE_COMPLIANCE, MODULE_IDENTITY, NOTIFICATION_GROUP, NOTIFICATION_TYPE, NOTIFICATIONS,
            OBJECT_GROUP, OBJECT_IDENTITY, OBJECT_TYPE, OBJECTS, OF, OPTIONAL, ORGANIZATION, PLUS_INFINITY, PRESENT,
            PRIVATE, PRODUCT_RELEASE, REFERENCE, REVISION, SEQUENCE, SET, SIZE, STATUS, SUPPORTS, SYNTAX, TAGS,
            TEXTUAL_CONVENTION, TRAP_TYPE, TRUE, UNITS, UNIVERSAL, VARIABLES, VARIATION, WITH, WRITE_SYNTAX);

    public static final TokenSet BUILTIN_TYPES = TokenSet.create(INTEGER, OBJECT_IDENTIFIER, OCTET_STRING);

}
